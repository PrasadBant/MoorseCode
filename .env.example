# Copy this file to .env.local (git-ignored) to point the dashboard at a
# real Pi gateway instead of the demo IP baked into config/env.js.
# See "How It Works" in the README for what this endpoint is expected to
# return.

VITE_API_ENDPOINT=http://10.56.55.74:5000/data
