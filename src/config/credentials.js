/**
 * credentials.js
 * Demo operator credentials checked by AdminLogin — see the "Security
 * Notes" section of the README before using any of this beyond a local
 * demo. There's no backend, so this list ships in the JS bundle and is
 * trivially visible via devtools; it is not a real access-control layer.
 */

export const VALID_CREDENTIALS = [
  { user: 'admin',    pass: 'ghost2024' },
  { user: 'operator', pass: 'rescue915' },
  { user: 'vishu',    pass: 'moorscode' },
];
